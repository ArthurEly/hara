# Conteúdo de /home/user/meus_modelos/SOYBEAN_CNV_2W2A.yaml

# Indica que o modelo vem da biblioteca externa "brevitas_examples"
model_source: brevitas_example

# Nome base do modelo para a função `model_with_cfg` da Brevitas
model_name: SOYBEAN_CNV 

# ID que você designou para o pipeline de transformação do CNV em hw_utils.py
topology_id: SOYBEAN_CNV

# Largura de bits para que o script saiba como construir o nome completo
# do modelo e passar os parâmetros adiante.
bit_widths:
  weight: 2
  activation: 2